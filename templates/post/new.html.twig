{% extends 'base.html.twig' %}

{% block title %}Crear Post{% endblock %}

{% block body %}
    {% include 'components/back_arrow.html.twig' %}
    
    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="w-full max-w-md p-8 bg-gray-800 rounded-lg shadow-xl">
            <h1 class="mb-6 text-4xl font-bold text-center text-teal-500">Crear Nuevo Post</h1>

            {{ form_start(form, {'attr': {'class': 'space-y-6', 'data-turbo' : 'false'}}) }}

            <div>
                <label for="title" class="text-lg font-medium text-orange-200/80">Título</label>
                {{ form_widget(form.title, {
                    'attr': {'class': 'mt-1 block w-full py-2 border-2 border-teal-200 focus:border-gray-600 rounded-md shadow-sm'}
                }) }}
            </div>

            <div>
                <label for="description" class="text-lg font-medium text-orange-200/80">Descripción</label>
                {{ form_widget(form.description, {
                    'attr': {'class': 'mt-1 block w-full py-2 border-2 border-teal-200 focus:border-gray-600 rounded-md shadow-sm'}
                }) }}
            </div>

            <div>
                <label for="image" class="text-lg font-medium text-orange-200/80">Imagen</label>
                {{ form_widget(form.image, {
                    'attr': {'class': 'mt-1 block w-full py-2 border-2 border-teal-200 focus:border-gray-600 rounded-md shadow-sm'}
                }) }}
            </div>

            {{ form_widget(form.submit, {'attr': {'class': 'w-full px-4 py-3 font-bold text-white transition duration-300 ease-in-out transform bg-teal-800 rounded-full hover:bg-orange-400/80 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50'}}) }}

            {{ form_end(form) }}

            <div class="mt-4 text-center">
                <a href="{{ path('app_posts') }}" class="text-teal-500 hover:underline">Volver a la lista de posts</a>
            </div>
        </div>
    </div>
{% endblock %}